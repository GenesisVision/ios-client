//
// NewParticipant.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation



open class NewParticipant: Codable {

    public var email: String
    public var name: String
    public var ethAddress: String
    public var avatar: String?


    
    public init(email: String, name: String, ethAddress: String, avatar: String?) {
        self.email = email
        self.name = name
        self.ethAddress = ethAddress
        self.avatar = avatar
    }
    

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {

        var container = encoder.container(keyedBy: String.self)

        try container.encode(email, forKey: "email")
        try container.encode(name, forKey: "name")
        try container.encode(ethAddress, forKey: "ethAddress")
        try container.encodeIfPresent(avatar, forKey: "avatar")
    }

    // Decodable protocol methods

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: String.self)

        email = try container.decode(String.self, forKey: "email")
        name = try container.decode(String.self, forKey: "name")
        ethAddress = try container.decode(String.self, forKey: "ethAddress")
        avatar = try container.decodeIfPresent(String.self, forKey: "avatar")
    }
}

