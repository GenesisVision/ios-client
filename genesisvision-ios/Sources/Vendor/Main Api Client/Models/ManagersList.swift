//
// ManagersList.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation



open class ManagersList: Codable {

    public var managers: [ManagerProfile]?
    public var total: Int?


    
    public init(managers: [ManagerProfile]?, total: Int?) {
        self.managers = managers
        self.total = total
    }
    

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {

        var container = encoder.container(keyedBy: String.self)

        try container.encodeIfPresent(managers, forKey: "managers")
        try container.encodeIfPresent(total, forKey: "total")
    }

    // Decodable protocol methods

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: String.self)

        managers = try container.decodeIfPresent([ManagerProfile].self, forKey: "managers")
        total = try container.decodeIfPresent(Int.self, forKey: "total")
    }
}

