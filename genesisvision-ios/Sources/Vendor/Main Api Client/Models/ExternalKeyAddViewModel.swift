//
// ExternalKeyAddViewModel.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation



open class ExternalKeyAddViewModel: Codable {

    public var title: String
    public var exchangeId: UUID
    public var key: String
    public var secret: String?


    
    public init(title: String, exchangeId: UUID, key: String, secret: String?) {
        self.title = title
        self.exchangeId = exchangeId
        self.key = key
        self.secret = secret
    }
    

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {

        var container = encoder.container(keyedBy: String.self)

        try container.encode(title, forKey: "title")
        try container.encode(exchangeId, forKey: "exchangeId")
        try container.encode(key, forKey: "key")
        try container.encodeIfPresent(secret, forKey: "secret")
    }

    // Decodable protocol methods

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: String.self)

        title = try container.decode(String.self, forKey: "title")
        exchangeId = try container.decode(UUID.self, forKey: "exchangeId")
        key = try container.decode(String.self, forKey: "key")
        secret = try container.decodeIfPresent(String.self, forKey: "secret")
    }
}

