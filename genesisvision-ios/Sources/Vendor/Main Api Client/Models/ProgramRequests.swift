//
// ProgramRequests.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation



open class ProgramRequests: Codable {

    public var requests: [ProgramRequest]?
    public var total: Int?
    public var totalValue: Double?


    
    public init(requests: [ProgramRequest]?, total: Int?, totalValue: Double?) {
        self.requests = requests
        self.total = total
        self.totalValue = totalValue
    }
    

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {

        var container = encoder.container(keyedBy: String.self)

        try container.encodeIfPresent(requests, forKey: "requests")
        try container.encodeIfPresent(total, forKey: "total")
        try container.encodeIfPresent(totalValue, forKey: "totalValue")
    }

    // Decodable protocol methods

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: String.self)

        requests = try container.decodeIfPresent([ProgramRequest].self, forKey: "requests")
        total = try container.decodeIfPresent(Int.self, forKey: "total")
        totalValue = try container.decodeIfPresent(Double.self, forKey: "totalValue")
    }
}

